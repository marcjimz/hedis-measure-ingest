command:
  - /bin/sh
  - -c
  - npm run build && npm start -- -p $DATABRICKS_APP_PORT

env:
  - name: NEXT_PUBLIC_API_URL
    value: $BACKEND_API_URL
  - name: NODE_ENV
    value: production
  - name: HOSTNAME
    value: 0.0.0.0
